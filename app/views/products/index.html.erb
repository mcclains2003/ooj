<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <h3><%= link_to(product.name, product) %></h3>
      <%= product.description %>
    </li>
  <% end %>
</ul>

<%= link_to("Add new product", new_product_path) %>